name: flutter_aws_workspace
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # AWS
  amplify_api: ^1.0.0
  amplify_flutter: ^1.0.0
  amplify_datastore: ^1.0.0-supports-only-mobile
  aws_dax_api: ^2.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
